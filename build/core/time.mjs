/* ============================================================
   build/core/time.mjs · OWNER: Agent A (core engine)
   The build uses the same time model as the client: re-export the pure
   library so there is exactly one implementation (site/js/lib/time.js).
   ============================================================ */
export * from "../../site/js/lib/time.js";

/* site/js/features/faq.js · OWNER: Agent G · STUB landed by Agent A.
   main.js imports this module when <body data-features> lists "faq" and calls init(app).
   app = { root, now(), store, plan, modal, toast, data(name), onTick(fn), status } (see site/js/main.js). */
export function init() {}
